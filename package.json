{
	"name": "scorm2cms-prototype",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"build": "rm -rf dist/* && tsc && TARGET=parent vite build && TARGET=wrapper vite build",
		"dev": "vite",
		"postinstall": "patch-package",
		"preview": "vite preview"
	},
	"devDependencies": {
		"patch-package": "^8.0.0",
		"typescript": "^5.5.3",
		"vite": "^5.4.0"
	},
	"dependencies": {
		"highlight.js": "^11.10.0",
		"scorm-again": "^1.7.1"
	}
}
