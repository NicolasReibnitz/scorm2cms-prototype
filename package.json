{
	"name": "scorm2cms-prototype",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"build": "rm -rf dist/* && tsc -p tsconfig.build.json && TARGET=parent vite build && TARGET=wrapper vite build && cp dist/wrapper/index.html dist/interactive/wrapper.html && cp dist/parent/index.html dist/interactive/parent.html",
		"dev": "vite",
		"postinstall": "patch-package",
		"preview": "vite preview",
		"server": "node server/index.cjs"
	},
	"devDependencies": {
		"patch-package": "^8.0.0",
		"typescript": "^5.5.3",
		"vite": "^5.4.0",
		"vite-plugin-singlefile": "^2.1.0"
	},
	"dependencies": {
		"body-parser": "^1.20.3",
		"express": "^4.21.1",
		"highlight.js": "^11.10.0",
		"scorm-again": "^2.5.0"
	}
}
